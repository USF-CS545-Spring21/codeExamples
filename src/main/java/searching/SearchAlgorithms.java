package searching;

public class SearchAlgorithms {

    public static boolean linearSearch(int[] arr, int start, int elem) {

        // fill in code
        return false;
    }

    public static boolean binarySearch(int[] arr, int start, int end, int elem) {

        int mid = (start + end) / 2;

        // fill in code
        return false;
    }

    public static void main(String[] args) {
        int[] arr = {8, 10, 11, 25, 50, 67, 90};
        binarySearch(arr, 0, arr.length - 1, 68);
    }
}
